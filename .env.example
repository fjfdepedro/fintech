# Database - Prisma Accelerate (includes automatic connection pooling and caching)
# ✅ For Prisma Accelerate, use this format (NO need for connection pool params):
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=your-accelerate-api-key"

# ✅ DIRECT_URL is required for Prisma migrations and Studio
# Get this from your Prisma Postgres dashboard
DIRECT_URL="postgresql://user:password@your-host.db.prisma-data.net:5432/dbname"

# API Keys
QWEN_API_KEY="your-qwen-api-key"
NEWSDATA_API_KEY="your-newsdata-api-key"

# Optional API Keys (recommended to reduce database queries)
MESSARI_API_KEY="your-messari-api-key" # Optional: Messari metrics (if not set, will use cached data only)
COINGECKO_API_KEY="your-coingecko-api-key" # Optional: Improves rate limits 